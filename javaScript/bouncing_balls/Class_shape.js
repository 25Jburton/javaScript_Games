class Shape {
    constructor(x, y, velX, velY){
        // Coordinates
        this.x = x;
        this.y = y;
        // Horizontal & Vertical Velocity
        this.velX = velX;
        this.velY = velY;
    }
}